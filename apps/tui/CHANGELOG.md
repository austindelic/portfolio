# Changelog

## 0.1.1

- Consume `austindelic-blackhole` and `austindelic-blackhole-ratatui` 0.1.0 instead of bundled renderer source.
- Keep the `npx austindelic` command and all five native targets; move repository and release metadata to `austindelic/portfolio`.
- Preserve adjustable Explore render resolution, static fallback, terminal-background support, content navigation, and terminal cleanup.
- Include current portfolio posts and release recovery fixes that publish the tested source snapshot.
- Distribute the bundled application under GPL-3.0-only with retained dependency and shader notices.
